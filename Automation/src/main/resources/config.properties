# Browser to run tests on (chrome, firefox, edge)
browser=chrome

# Base URL of nopCommerce
url=https://demo.nopcommerce.com

# Timeout settings in seconds
implicitWait=10
pageLoadTimeout=30

# Capture screenshots for passed tests
capturePassedScreenshots=true
